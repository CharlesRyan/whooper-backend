service: whooper-test

provider:
  name: aws
  runtime: python3.6
  region: us-west-1

functions:
  numpy:
    handler: handler.main

plugins:
  - serverless-python-requirements

custom:
  pythonRequirements:
    zip: true
    layer: true
    dockerizePip: non-linux
